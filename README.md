# Java-Product_Of_Factors_Prime_Or_Not
This Java program calculates the product of all factors of a given number and checks if the product is a prime number. It uses loops to find the factors and a helper method isPrime() to determine primality. The result is printed indicating whether the product is prime or not.
